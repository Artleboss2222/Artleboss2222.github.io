name: Discord Notification
on: [push]

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Send Message
        uses: appleboy/discord-action@master
        with:
          webhook_id: ${{ secrets.DISCORD_ID }}
          webhook_token: ${{ secrets.DISCORD_TOKEN }}
          message: |
            **${{ github.actor }}** a push sur le repo !
            📝 **Message:** ${{ github.event.head_commit.message }}
            📂 **Fichiers modifiés:** ${{ join(github.event.head_commit.modified, ', ') }}
            🔗 [Voir les changements](${{ github.event.head_commit.url }})
