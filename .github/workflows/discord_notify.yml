name: Discord Notification
on: [push]

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Send Enhanced Message
        uses: appleboy/discord-action@master
        with:
          webhook_id: ${{ secrets.DISCORD_ID }}
          webhook_token: ${{ secrets.DISCORD_TOKEN }}
          color: "#2f81f7"  # Bleu GitHub stylé
          username: "GitHub Bot"
          avatar_url: "https://github.githubassets.com/images/modules/logos_page/GitHub-Mark.png"
          message: |
            ### 🚀 Nouveau Push sur le dépôt !
            
            👤 **Auteur :** `${{ github.actor }}`
            💬 **Message :** > ${{ github.event.head_commit.message }}
            
            ---
            📂 **Fichiers impactés :**
            `${{ join(github.event.head_commit.modified, '`, `') }}`
            
            ✨ **Stats :** ✅ `${{ github.ref_name }}` branch
            📦 `${{ github.repository }}`
            
            🔗 [Clique ici pour voir le code](${{ github.event.head_commit.url }})
